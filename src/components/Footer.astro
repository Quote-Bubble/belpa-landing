---
const year = new Date().getFullYear();
---

<!-- Sharp closer: sends people back to the address field -->
<section
  class="footer-cta cv-auto relative mx-auto max-w-3xl px-4 pt-5 pb-6 text-center sm:px-6 sm:pt-8 sm:pb-16"
  data-reveal="scale"
>
  <p class="footer-cta-line font-display">Want to give your customers instant roof quotes?</p>
  <button type="button" data-try class="footer-cta-btn">
    Try now
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <path d="M12 19V5M5 12l7-7 7 7" stroke="currentColor" stroke-width="2.25" stroke-linecap="round" stroke-linejoin="round"/>
    </svg>
  </button>
</section>

<footer class="footer-giant relative">
  <!-- Desktop: giant brand echo + scroll FX. Hidden on phones. -->
  <div class="giant-wrap" data-reveal="brand-echo">
    <img
      class="giant-word"
      src="/belpa-wordmark-light.png"
      alt=""
      width="1329"
      height="412"
      aria-hidden="true"
      data-fx="punch"
    />
  </div>

  <!-- Mobile-only closer: always visible, no reveal/FX dependency. -->
  <div class="footer-m" aria-hidden="true">
    <img
      class="footer-m-brand"
      src="/belpa-wordmark-light.png"
      alt=""
      width="1329"
      height="412"
    />
  </div>

  <div class="footer-legal">
    <div class="footer-legal-links">
      <a href="/terms">Terms of Service</a>
      <a href="/privacy">Privacy Policy</a>
    </div>
    <span class="footer-legal-copy">© {year} Belpa. All rights reserved.</span>
  </div>
</footer>

<style>
  .footer-cta-line {
    margin: 0;
    font-size: clamp(1.6rem, 4.5vw, 2.35rem);
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1.1;
    color: var(--color-ink);
  }

  .footer-cta-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    margin-top: 1.15rem;
    padding: 0.9rem 1.55rem;
    border: 0;
    border-radius: 999px;
    background: linear-gradient(
      180deg,
      var(--color-brand-500),
      var(--color-brand-600)
    );
    font-size: 1.05rem;
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1;
    color: #fff;
    cursor: pointer;
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.28) inset,
      0 12px 28px -10px rgba(31, 87, 240, 0.55);
    transition:
      filter 0.15s ease,
      transform 0.2s var(--ease-out-soft, ease),
      box-shadow 0.2s ease;
  }
  .footer-cta-btn:hover {
    filter: brightness(1.06);
    transform: translateY(-1px);
    box-shadow:
      0 1px 0 rgba(255, 255, 255, 0.3) inset,
      0 16px 32px -10px rgba(31, 87, 240, 0.6);
  }
  .footer-cta-btn:active {
    transform: translateY(0);
    filter: brightness(0.98);
  }
  .footer-cta-btn:focus-visible {
    outline: 2px solid var(--color-brand-700);
    outline-offset: 3px;
  }
  .footer-cta-btn svg {
    flex: none;
  }

  /* —— Desktop footer (unchanged drama) —— */
  .footer-giant {
    overflow: hidden;
    background: linear-gradient(
      180deg,
      rgba(255, 255, 255, 0) 0%,
      #eaf1ff 24%,
      #bcd4ff 55%,
      #7dabff 82%,
      #4f8bff 100%
    );
  }

  .giant-wrap {
    display: flex;
    justify-content: center;
    padding-inline: 0.5rem;
  }

  /* The logo as a white silhouette rather than type set to imitate it — same
     device (and same asset) as the dashboard's auth echo. Height is tuned to
     land at roughly the width the type version occupied. */
  .giant-word {
    /* Held in a custom property so the negative margin can be a fraction of
       the rendered height. `em` would resolve against the inherited font-size
       here (an image has none of its own), which is ~1px, not ~11px. */
    --giant-h: clamp(2.6rem, 14.3vw, 15.5rem);
    display: block;
    /* .giant-wrap is a flex container: without flex:none the default shrink
       would narrow the img while the height stays fixed, squashing the logo. */
    flex: none;
    height: var(--giant-h);
    width: auto;
    max-width: none;
    margin-top: clamp(1.25rem, 4vw, 3rem);
    /* Tucks the base behind the legal bar, as the type version's cropped
       descender did. */
    margin-bottom: calc(var(--giant-h) * -0.055);
    opacity: 0.92;
    user-select: none;
    pointer-events: none;
  }

  .footer-m {
    display: none;
  }

  .footer-legal {
    position: relative;
    z-index: 5;
    background: #2f6bff;
    padding: 14px 20px calc(14px + env(safe-area-inset-bottom, 0px));
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 6px;
    text-align: center;
    font-size: 12.5px;
  }
  .footer-legal-links {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: 6px 22px;
  }
  .footer-legal a {
    color: #ffffff;
    transition: opacity 0.15s ease;
  }
  .footer-legal a:hover {
    opacity: 0.88;
    text-decoration: underline;
  }
  .footer-legal-copy {
    color: rgba(255, 255, 255, 0.92);
  }

  /* —— Mobile-only remake ——
     One solid blue slab: brand → links → copyright. No scroll-reveal, no FX,
     no empty gradient padding. Always painted, always readable. */
  @media (max-width: 640px) {
    .footer-cta {
      padding-top: 1.1rem;
      padding-bottom: 1.35rem;
    }
    .footer-cta-line {
      font-size: 1.45rem;
    }
    .footer-cta-btn {
      margin-top: 0.85rem;
      padding: 0.85rem 1.35rem;
      font-size: 1rem;
    }

    .footer-giant {
      background: #2f6bff;
      /* Hairline dissolve off the white page — not a tall pale ramp */
      box-shadow: 0 -28px 40px -28px rgba(47, 107, 255, 0.55);
    }

    .giant-wrap {
      display: none;
    }

    .footer-m {
      display: block;
      padding: 1.35rem 1.25rem 0.15rem;
      text-align: center;
    }
    .footer-m-brand {
      display: block;
      height: 2.6rem;
      width: auto;
      margin: 0 auto;
      user-select: none;
    }

    .footer-legal {
      background: transparent;
      padding: 0.55rem 1.25rem calc(1.1rem + env(safe-area-inset-bottom, 0px));
      gap: 0.45rem;
      font-size: 12px;
    }
    .footer-legal-links {
      gap: 0.35rem 1.35rem;
    }
    .footer-legal a,
    .footer-legal-copy {
      color: #ffffff;
    }
    .footer-legal-copy {
      opacity: 0.88;
    }
  }
</style>
