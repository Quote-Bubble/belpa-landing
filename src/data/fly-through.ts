/**
 * The fly-through phrase, one entry per line.
 *
 * Kept short deliberately: at 12rem a third line does not fit a laptop
 * viewport, and the effect depends on the type filling the screen rather than
 * sitting inside it.
 */
export const WORDS = ["Measured", "from space"];
